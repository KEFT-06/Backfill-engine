# Schema eras

> The eras of the GH Archive event schema, **discovered** from the data itself
> (`scripts/discover_schema_eras.py`), not assumed. Filled in Phase 4.

For each era: the date boundary, what changed at that boundary, and how the
versioned parser maps it onto the canonical output schema.

| Era | From | To | What changed | Parser |
|-----|------|----|--------------|--------|
| _tbd_ | | | | |

<!-- Discovery method, evidence, and mapping strategy go here in Phase 4. -->
